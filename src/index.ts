// @333eco/primitives — the estate's recomputable functions, one module per
// primitive. Import the module you mean (`@333eco/primitives/b-call`); this
// barrel exists so the package root is not a dead end.
export * as bCall from "./b-call.js";
